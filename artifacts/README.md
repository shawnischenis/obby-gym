# Policy artifacts

Generated policy weights and export bundles are written here and ignored by Git. Each bundle must include architecture metadata, preprocessing parameters, schema hashes, and golden parity vectors.
